{
  "name": "vizdiff",
  "version": "0.0.0",
  "private": true,
  "packageManager": "yarn@3.5.0",
  "workspaces": [
    "api",
    "cli",
    "frontend",
    "frontend2",
    "worker"
  ],
  "scripts": {
    "api": "yarn workspace api",
    "cli": "yarn workspace cli",
    "chromatic": "yarn workspace frontend run chromatic",
    "frontend": "yarn workspace frontend",
    "frontend2": "yarn workspace frontend2",
    "worker": "yarn workspace worker",
    "build:all": "yarn workspaces foreach -p run build",
    "test:all": "yarn workspaces foreach -p run test",
    "lint:all": "yarn workspaces foreach -p run lint"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
